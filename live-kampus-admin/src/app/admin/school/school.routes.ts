import { Routes } from '@angular/router';
import { SchoolListComponent } from './school-list.component';

export const schoolRoutes: Routes = [
    { path: "admin/schoollist", component: SchoolListComponent }
];